language: java
install: true

jdk:
- openjdk11

sudo: false

cache:
  directories:
    - $HOME/.m2
    - $HOME/.gradle

script:
  - ./gradlew build

deploy:
  - provider: script
    script: ./gradlew bintrayUpload
    skip_cleanup: true
    dry-run: false
    on:
      tags: true
      branch: master
